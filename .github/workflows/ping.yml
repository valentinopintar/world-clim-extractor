name: Keep Streamlit App Alive

on:
  schedule:
    # Pokreće se svakih 11 sati
    - cron: "0 */11 * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit app
        run: |
          curl -s https://world-clim-extractor.streamlit.app/ > /dev/null
